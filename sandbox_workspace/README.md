# Sandbox Workspace — Sample Project

A tiny Python project used to test the Multi-Agent Issue Resolver.

Contains a known bug: `calculate_total([])` in `src/utils.py` crashes
with a `TypeError` when passed an empty list.
